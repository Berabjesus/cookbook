<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Food Details</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <nav>
    <a href="/">Cookbook</a>
    <a href="/auth/signout">Sign out</a>
  </nav>

  <h1>Food Details</h1>

  <p>Name: <%= food.name %></p>
  <p>Quantity: <%= food.quantity %></p>

  <a href="/users/<%= userId %>/foods/<%= food._id %>/edit">Edit Food</a>

  <form action="/users/<%= userId %>/foods/<%= food._id %>?_method=DELETE" method="post">
    <button type="submit">Delete Food</button>
  </form>
  <a href="/users/<%= userId %>">Back to pantry</a>
</body>
</html>